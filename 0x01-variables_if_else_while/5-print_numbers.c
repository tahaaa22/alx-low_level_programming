#include <stdio.h>


/**
 * main - Entry point
 *
 * Description: prints all single digit numbers
 *
 * Return: 0 (Success)
*/

int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i", n);
		n++;
	}

	return (0);
}
